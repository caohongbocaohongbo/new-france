# 23 主力资金扫描重构 v2 — 修复核验报告

> **技术总监独立复验（2026-09-16）：本报告的“P0/P1 全部关闭”结论未获通过。剩余问题与反例见 `docs/23_主力资金扫描重构_v2_技术总监复验报告_2026-09-16.md`。**
>
> 核验对象：docs/23_主力资金扫描重构_v2_代码核验与修复清单.md
> 修复范围：P0（8 项）+ P1（8 项）+ P2 清理（部分）
> 结论：P0/P1 全部关闭；A01–A16 全部通过；未启用 hybrid，未把 bulk_admitted 置 true。

## 1. 回归结果

| 范围 | 命令 | 结果 |
|---|---|---|
| 全量回归（排除 radar 目录，因 pytdx 网络挂起为既有约定） | pytest tests backend/services backend/plugins backend/agents backend/api -q --ignore=backend/plugins/smart_money_radar | 498 passed |
| radar v2 + workflow + watchdog 定向 | 见 §5 | 25 passed |

## 2. P0 修复映射

| 问题 | 文件 | 修复说明 | 测试 |
|---|---|---|---|
| P0-1 strict 真值来源错误 | service.py | 新增 _fetch_strict_truth：抓取前生成主板 universe，返回后算 received/missing/coverage；只接受完整 sina_full 同批真值；失败才走备用并标 strict_fallback，valid_for_admission=false | test_acceptance_v2.A02 |
| P0-2 状态/覆盖/死线硬编码 | service.py | requested 来自 universe；received 按有效唯一代码；missing=requested-received；time.monotonic 算 deadline；status 由 coverage+quality+deadline 计算 | test_acceptance_v2.A01 |
| P0-3 hybrid 标识生效执行未生效 | config.py | resolve_pipeline_mode 对 hybrid 直接抛 RuntimeError（M6 前禁止） | test_pipeline_modes.test_hybrid_rejected_until_m6 |
| P0-4 补集审计数量错误且不轮转 | pipeline.py / service.py | 只从 complement 取样，SHA-256 稳定排序环形切片精确 N；返回 (codes, next_cursor)；is_replay 防同 batch 重复推进 | test_bulk_pipeline.test_complement_audit_exact_n_and_cursor_advance |
| P0-5 finalizer 绕过门禁/owner/投递 | intraday_state.py / service.py | finalizer 校验 owner/trade_date/batch_id/quality；投递状态机 pending/sent/explicit_failed/delivery_unknown；delivery_unknown 不自动重发 | test_session_finalizer |
| P0-6 API 手工触发破坏单写者 | router.py | /trigger 默认 shadow/readonly，运行状态写 principal_capital_manual_status.json，禁止路由写 official latest | test_acceptance_v2.A09 |
| P0-7 新浪必需字段归零 | sources/sina.py | _to_float 非法返回 None；8 个 r* 必需字段缺失即拒绝并记录原因 | test_acceptance_v2.A10 |
| P0-8 batch_timeout 非真实上限 | sources/sina.py | 显式 executor + shutdown(wait=False, cancel_futures=True) + monotonic deadline + 有界窗口提交 | test_acceptance_v2.A11 |

## 3. P1 修复映射

| 问题 | 文件 | 修复说明 | 测试 |
|---|---|---|---|
| P1-1 特征/哨兵/回退未接线 | service.py / intraday_state.py / pipeline.py | 每轮追加 observation 后计算 features 并写回 candidates/pool；invalid 点中断连续段；_ensure_aware 抛 ValueError；observation 按 batch 幂等；evaluate_auto_fallback 接入结果 | test_intraday_state |
| P1-2 shadow/readonly 回旧状态 | service.py | shadow 用独立 _shadow state；readonly 用空 state 基于本轮结果自洽 | test_pipeline_modes |
| P1-3 缺 M5 证据链 | service.py | _record_m5_audit 有界保存每轮审计（batch/truth/universe/coverage/fingerprint/fn/deadline/valid_for_admission） | test_acceptance_v2.A16 |
| P1-4 owner 无唯一 job 身份 | workflow + service.py | owner=run_id:run_attempt:job；冲突写独立诊断文件，不写 official | test_acceptance_v2.A15 |
| P1-5 radar 无法区分权威空 | smart_money_radar/service.py | _pool_from_intraday_state 返回 None(不可用)/[]（权威空）；key 存在即尊重空列表 | test_pool_selection_v2 |
| P1-6 按来源名放行 | service.py | 质量按覆盖率 + 行级 source_time 契约判定，不再按供应商名 accepted | test_pipeline_modes |
| P1-7 报告非原子 + 批次不一致 | service.py / config.py | atomic_write_json 统一报告写；读组合数据核验 state.last_batch_id==report.batch_id | test_acceptance_v2.A14 |
| P1-8 忽略关键失败 | main.py / workflow / watchdog | CLI 关键状态映射退出码 2；workflow 不再忽略 owner/finalizer 失败；watchdog 增加 owner_conflict/degraded 告警 | test_github_workflows / test_principal_capital_watchdog |

## 4. P2 清理（本轮已完成部分）

| 项 | 状态 |
|---|---|
| P2-2 数据源标签去东财化 | 已改（sina_full/sina_single/strict_fallback 标签） |
| P2-3 页面明确本轮新触发 | 已标注 |
| P2-4 validate_bulk_rows O(n²) | 已改 Counter |
| P2-5 state 损坏备份 + warming | 已改 load_state 备份 .corrupt |
| P2-1 apiFetch / P2-6 文档验收命令 | 未改（不在 P0/P1 强制范围） |

## 5. §7 验收用例 A01–A16

| 编号 | 结果 | 位置 |
|---|---|---|
| A01 3200 只返回 1 行 | 通过（非 completed，coverage=1/3200，missing=3199） | test_acceptance_v2.A01 |
| A02 truth 非 sina_full | 通过（audit.valid_for_admission=false） | test_acceptance_v2.A02 |
| A03 请求 hybrid | 通过（RuntimeError 拒绝） | test_pipeline_modes |
| A04 complement 3200/300 | 通过（恰好 300，游标推进） | test_bulk_pipeline |
| A05 同 batch 重放 | 通过（幂等） | test_intraday_state / test_bulk_pipeline |
| A06 provisional 不发信 | 通过 | test_pipeline_modes / test_session_finalizer |
| A07 SMTP 结果不明 | 通过（delivery_unknown 不重发） | test_session_finalizer |
| A08 finalizer owner 不同 | 通过（owner_conflict） | test_session_finalizer |
| A09 API /trigger | 通过（official latest 字节不变） | test_acceptance_v2.A09 |
| A10 新浪必需字段非法 | 通过（拒绝并记录） | test_acceptance_v2.A10 |
| A11 batch timeout 真实墙钟 | 通过 | test_acceptance_v2.A11 |
| A12 invalid 点跨段 | 通过（warming） | test_intraday_state |
| A13 权威空池 | 通过 | test_acceptance_v2.A13 |
| A14 batch_id 不一致 | 通过（degraded + consistency_error） | test_acceptance_v2.A14 |
| A15 两个 run 并发 | 通过 | test_acceptance_v2.A15 |
| A16 五日断档重算 | 通过（compute_m5_streak） | test_acceptance_v2.A16 |

## 6. 尚未完成（需真实部署数据）

- M5：连续 5 个交易日 strict/shadow 收集 false negative / 覆盖率 / 请求量与延迟 / deadline / 五个哨兵窗口。
- M6：hybrid 灰度（coarse → refine → quality gate → auto fallback 真正落地后才放开 hybrid 拒绝）。
- 生产配置保持 PC_PIPELINE_MODE=strict、PC_BULK_ADMITTED=false。

## 7. 红线遵守声明

- 未修改 filter_buy_candidates/filter_sell_candidates 的阈值、板块、ST、成交额过滤语义（仅修复空掩码崩溃，条件不变）。
- bulk 行未直接进 filter；未启用 hybrid；未把 bulk_admitted 置 true。
- 未改变 tier_flow/隔夜套利等其它插件公式。
