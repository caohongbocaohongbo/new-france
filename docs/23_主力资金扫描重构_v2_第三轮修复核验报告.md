# 23 主力资金扫描重构 v2 — 第三轮修复核验报告

> 复验对象：docs/23_主力资金扫描重构_v2_技术总监复验报告_2026-09-16.md
> 结论：第三轮 P0-R1~R5 / P1-R1~R8 已修复；未启用 hybrid，未启动 M5，bulk_admitted 保持 false。

## 回归结果

| 命令 | 结果 |
|---|---|
| pytest tests backend/services backend/plugins backend/agents backend/api -q --ignore=backend/plugins/smart_money_radar | 512 passed |
| pytest radar v2 + workflow + watchdog | 25 passed |
| compileall backend/plugins/principal_capital backend/plugins/smart_money_radar scripts/principal_capital_watchdog.py | OK |
| git diff --check | 无输出（通过） |

## 问题 → 文件 → 位置 → 修复 → 测试 → 反例前/后

| 问题编号 | 修改文件 | 生产代码位置 | 修复说明 | 集成测试 | 修复前反例 | 修复后 |
|---|---|---|---|---|---|---|
| P0-R1 | pipeline.py / service.py | compute_round_valid；run_principal_capital_scan 的 round_valid | 单轮准入由完整条件计算（truth= sina_full + universe_verified + coverage==1.0 + 无 rejected + bulk shadow_only + bulk validation valid + 零 FN + deadline_met），不再复制 truth 标记 | test_integration_v3.RoundValidTest | shadow_error_admission=True | shadow_error -> valid_for_admission=False |
| P0-R1 | pipeline.py / service.py | compute_m5_streak；_record_m5_audit | 按交易日聚合：任何无效轮次使当日无效、配置指纹唯一、am+pm 覆盖；使用项目交易日历跳过周末/休市日；audit 保存最终 round_valid | test_acceptance_v2.A16；test_integration_v3.M5StreakTest | 同日一失败仍连续；周五->周一 streak=1 | 同日任一失败该日无效；周末不打断 |
| P0-R2 | commit/restore 脚本 | scripts/commit_screening_data.sh / restore_screening_data.sh | 将 m5_audit / intraday_state_shadow / owner_conflict 纳入 commit、restore、重建白名单 | tests/test_github_workflows.py | 审计跨 job 丢失 | 白名单含 3 个文件 |
| P0-R3 | intraday_state.py / service.py | should_finalize_session / finalize_principal_capital_session | 默认 not_attempted；pending+attempt_id 重启解释为 delivery_unknown；explicit_failed 仅 manual_retry；记录原/新 attempt_id | test_integration_v3.FinalizerCrashTest | pending_retry_decision={should_send:true} | 子进程写 pending 后重启 -> SMTP 不调用 |
| P0-R4 | intraday_state.py / service.py | acquire_owner_atomic | fcntl.flock 文件锁临界区内 load->check->persist lease；网络请求前已持久化 | test_integration_v3.OwnerAtomicTest | 两进程同时成为 writer | 两个 writer 仅一个成功 |
| P0-R5 | sina_market.py / service.py | fetch_main_board_universe | 分页返回 failed_pages/pages_fetched/terminal_page_seen/duplicate_count/verified；中间缺页 verified=false，不写新鲜权威缓存；strict report 输出 universe verification meta | test_sina_market.py | 缺页仍 100% 完整 | 缺页 -> verified=false |
| P1-R1 | service.py | run_principal_capital_scan | deadline 在 truth+bulk 结束后计算；truth/bulk/processing 分别记录；统一用于 status/fallback/M5 | test_integration_v3.ScanIntegrationTest.test_bulk_latency_over_deadline | deadline_excludes_bulk=True | bulk 超时 -> deadline_met=false |
| P1-R2 | service.py | 选池后回填 features | select_radar_pool 后统一把 features 写回 pool entry（protected + 新成员） | test_integration_v3.ScanIntegrationTest.test_features_reach_pool_and_radar | pool_features_after_refresh=None | pool latest_metrics.features 含 6 个字段 |
| P1-R3 | intraday_state.py | save_state / clean_state_for_save | 保存前 schema 清理，剔除 _source_unavailable / warming_reason 临时标记 | test_intraday_state.py | 首次无文件后 radar 永久回退 | 成功扫描后 state 不再含标记 |
| P1-R4 | service.py | _check_report_consistency / read_report_resilient | 所有带 batch_id 报告执行一致性检查；缺 state/空 last_batch_id/trade_date 不一致/schema 不兼容均 consistency_error；远程快照不与本地 state 混合判定 | test_integration_v3.ConsistencyTest | missing_state_consistency=completed | completed+缺 state -> degraded+consistency_error |
| P1-R5 | service.py / intraday_state.py | sentinel + auto_fallback | should_run_sentinel/mark_sentinel_done 接入主流程；auto_fallback 在 save_state 前赋值并持久化 | test_integration_v3.SentinelTest | sentinel 只存在测试 | sentinel_done 持久化且不重复 |
| P1-R6 | watchdog.py | evaluate_snapshot / run_watchdog | watchdog 读 owner_conflict / intraday_state / m5_audit；新增 finalizer_missing/pending_stuck/delivery_unknown/m5 告警 | tests/test_principal_capital_watchdog.py | conflict 检测不到 | watchdog 覆盖 conflict/summary/m5 |
| P1-R7 | service.py / pipeline.py | run_principal_capital_scan(batch_id) | 支持传入 batch_id 复用 RoundContext；is_replay 下 seen_rounds/audit_cursor/M5/history 不重复 | test_intraday_state / test_bulk_pipeline | 每次新 UUID 无法重放 | 同 batch 重放幂等 |
| P1-R8 | service.py / multi_source.py / sina_market.py / router.py | 原子写 + manual 终态 | history/notified/health/codes/fallback cache 统一 atomic_write_json；manual status 写 completed/partial/degraded/no_data/error + batch_id + 起止时间 + shadow_report_path；加并发保护与频率限制 | test_acceptance_v2.A09 | manual 永久 running | manual 写真实终态 |

## 尚未完成（需真实部署数据）

- M5 连续五个交易日证据收集（生产约束：禁止启动 M5）。
- M6 hybrid 灰度（继续代码层拒绝 hybrid）。

