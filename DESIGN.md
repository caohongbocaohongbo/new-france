# New France 设计规范

## 设计目标

New France 是面向交易日复盘与筛选的后台工具，视觉基调应接近百度财经：清爽灰底、白色内容面板、高密度表格、明确的红涨绿跌、低干扰的蓝色主操作。页面应优先保证扫描效率、数据可信度和操作路径清晰，不做营销式大图区块。

本版前端视觉以 Figma `france` 文件的 5 个业务画板为准：Dashboard 总览、监控列表、推荐结果、股票详情弹窗、手动筛选。列表画板中原有列错位不照抄，表格列宽按可读性和固定分页体验做实现修正。

## 基础样式

- 页面背景：`#ffffff`
- 左侧导航背景：`#f5f6fa`
- 卡片/面板：`#ffffff`
- 卡片浅灰底：`#f7f8fb`
- 主色：`#2d32e7`
- 主按钮深色：`#151024`
- A 股涨色：`#f5222d`
- A 股跌色：`#009a70`
- 警示/BUY：`#f5a400`
- 主文本：`#1a1a1a`
- 正文：`#333333`
- 次级文本：`#666666`
- 弱文本：`#999999`
- 边框：`#e3e6eb`
- 弱边框：`#eef0f4`
- 字体：`PingFang SC`, `Microsoft YaHei`, Apple/system sans-serif
- 数字字体：价格、百分比、计数使用 `font-variant-numeric: tabular-nums`，股票代码优先使用等宽字体。

## 布局

- Sidebar 宽度固定 220px，浅灰底，Logo 与导航按 Figma 37px 行高排列，状态和下次执行信息常驻底部。
- 顶部栏视觉高度 88px，左侧页面标题，右侧日期或数据源说明。
- 内容区使用 24px 左右边距，顶部与 Figma 画板保持高密度留白。
- 数据表格、推荐卡片、筛选表单均放在白色面板上。
- 业务卡片圆角为 8px；表单控件、按钮、标签使用 4px 小圆角，保持工具型界面克制。

## 表单与下拉框

- 输入框与下拉框高度统一为 36px。
- 下拉框必须清除浏览器默认外观，使用自定义下箭头，边框、字体、背景与输入框一致。
- 聚焦态使用主色边框和浅蓝外发光。
- 不使用 `alert()` 作为反馈，所有结果展示在页面内状态区域。

## 监控列表

- 表格列保持：代码、名称、涨停日期、参考价、现价、回撤%、换手率、量比、PE、封板时间、涨停次数、流通市值、状态、操作。
- 表头固定在列表滚动容器顶部。
- 分页区域固定在列表面板底部，表格内容过长时只滚动表格区域，避免用户滚到底部才看到翻页。
- 分页文案按 Figma 使用“上一页 / 第 x/y 页 / 下一页 / 共 n 条”，不展示完整页码矩阵。
- 查询条件来自输入框、状态下拉和排序表头，最终请求 `/api/v1/watchlist`。
- 从推荐结果跳转监控列表时，必须携带股票代码/名称到搜索条件并立即过滤。

## 推荐结果

- 页面顶部保留日期、等级筛选和统计卡片。
- 统计卡片本身是可点击 TAB：STRONG BUY / BUY / WATCH 分别过滤对应等级，总计评分恢复全部推荐；下拉框与卡片选中态必须保持同步。
- 推荐列表使用单条大卡片，不用普通数据表格承载推荐详情。
- 卡片结构：
  - 左侧排名、股票代码、名称。
  - 右侧评分、推荐等级标签、观察按钮。
  - 中部展示回撤、涨停日、关注日、关注至今涨停板次数、参考价、现价。
  - 审核降级信息使用浅红底和红色左边线。
  - 因子得分使用浅灰/浅蓝胶囊标签。
  - 趋势图展示“关注后交易日趋势图”，包含收盘价、回撤%、参考价和关注后涨停次数四类线索。
- 趋势图必须只使用接口返回的 `price_history` 或 `/api/v1/watchlist/{code}/detail` 的真实 K 线数据，不得用前端假数据补齐。
- 无价格历史时显示“接口未返回价格历史，未使用本地假数据补绘”。
- 关注至今涨停板次数来自监控列表接口的 `zt_count` / `follow_limit_up_count`，并在图表底部以次数摘要和真实涨停日期胶囊展示；接口没有历史涨停日期序列时只显示空态或当前监控记录，不得编造多次日期。

## 图表/个股详情

- 股票详情弹窗使用浅色遮罩与白色 1100px 宽内容容器，圆角 12px。
- 价格走势、成交量趋势、均线系统、每日涨跌幅、回撤分布统计均使用浅色 ECharts 配置：白底、弱网格线、浅灰坐标轴、白色 tooltip。
- 价格走势：收盘价蓝色、回撤线蓝紫色、参考价灰色虚线。
- 成交量与每日涨跌幅：红涨绿跌，柱体不使用深色背景。
- 均线系统：MA5 蓝、MA10 青蓝、MA20 青色、MA60 绿色。
- 回撤分布统计使用横向柱图，不使用饼图。
- 补充指标趋势包括换手率、量比、PE、封板时间、涨停板时间可视化。
- 换手率/量比/PE 仅在后端通过真实 Tushare 日频基础指标返回时绘制；接口缺失时显示空态，不用前端假数据补线。
- 封板时间趋势仅在接口提供历史序列时绘制。
- 涨停板时间至少展示当前监控记录中的真实涨停日期、封板时间和连板语义；不存在历史多次涨停事件时不得前端编造。

## 策略配置

当前前端策略配置页分为三类：

- 策略参数：保存按钮固定在策略参数面板底部，调用 `PUT /api/v1/config/strategy` 写入后端配置表，并同步 `config/strategy_params.py`。
- 通知配置：展示发件邮箱、收件邮箱、SMTP 主机、SMTP 端口；保存后测试邮件调用 `/api/v1/system/test-email`，实际发送时读取后端配置表中的收件账号。
- 定时任务：展示 cron 信息，手动触发调用 `/api/v1/screening/run`；未显式传筛选参数时，后端读取运行时配置。
- 因子权重：保存后进入后端配置表，总权重必须为 100%；多因子评分优先读取配置表中的权重。
- 上次修改比对：设置页底部展示“上次保存值 / 当前编辑值”，有变化的行使用浅黄底提示。

后端真正参与筛选的参数来自：

- 手动筛选页提交给 `/api/v1/screening/run` 的 query 参数。
- 未提交 query 参数时，读取 SQLite `system_configs` 表中的 `strategy_runtime`。
- 配置表不存在时，回退到 `config/strategy_params.py`。

配置表中的值优先级高于 `config/strategy_params.py`。前端保存成功后，后端会同步更新 `config/strategy_params.py`，确保文件默认值与最新配置一致。SMTP 密码不得出现在页面、接口响应、数据库或文档中，只能通过环境变量提供。

## 数据真实性

- 前端不得渲染硬编码业务样例数据。
- 加载态、错误态、空态可以显示文案，但不能构造股票、价格、评分或图表线。
- 监控列表数据来自 `data/france.md` 和后端实时行情补全。
- 推荐结果主体来自 `reports/latest.json`，上证指数点位来自后端行情源快照字段 `index_value` / `index_snapshot.value`，涨跌幅来自 `index_gain`。
- 推荐卡片关注日、关注至今涨停板次数来自 `/api/v1/watchlist` 和 `/api/v1/watchlist/{code}/detail`，字段为 `added_date`、`zt_count`、`follow_limit_up_count`。
- 价格历史来自后端历史 K 线与当前价构建的 `price_history`，推荐结果缺失 `price_history` 时允许用详情接口的真实 `chart_data` 补绘。
- 后端允许数据源降级，但应在审计报告或未来接口字段中暴露降级来源，避免把历史收盘价误认作实时价。

## 发布同步

前端代码源在 `frontend/`。静态发布镜像在 `docs/`。任何 UI、JS 行为、CSS 规范变化都必须同步：

- `frontend/index.html` 到 `docs/index.html`
- `frontend/css/styles.css` 到 `docs/css/styles.css`
- `frontend/js/app.js` 到 `docs/js/app.js`
